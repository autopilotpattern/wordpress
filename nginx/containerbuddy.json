{
  "consul": "consul:8500",
  "onStart": [
    "consul-template", "-retry", "5s", "-consul", "consul:8500", "-template",
    "/etc/containerbuddy/nginx.conf.ctmpl:/etc/nginx/nginx.conf"
  ],
  "services": [
    {
      "name": "nginx",
      "port": 80,
      "interfaces": ["eth1", "eth0"],
      "health": "/usr/bin/curl --fail -s http://localhost/health",
      "poll": 10,
      "ttl": 25
    }
  ],
  "backends": [
    {
      "name": "wordpress",
      "poll": 3,
      "onChange": [
        "consul-template", "-once", "-consul", "consul:8500", "-template",
        "/etc/containerbuddy/nginx.conf.ctmpl:/etc/nginx/nginx.conf:nginx -s reload"
      ]
    }
  ]
}
